<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.activy3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Activy3">
        <activity android:name=".ActivityLihatData" android:label="@string/detailKontak"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".HomeActivity" android:label="@string/dataKontak"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".ActivtyDaftar" android:label="@string/mDaftar"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ActivtyHasil"
            android:parentActivityName=".MainActivity"
            android:label="@string/label2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>